PORT=8080
# Long random value shared only with the companion.
RELAY_CLIENT_TOKEN=replace-with-at-least-32-random-characters
# Kick's documented RSA public key in PEM form. Use literal \n between lines.
KICK_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----

